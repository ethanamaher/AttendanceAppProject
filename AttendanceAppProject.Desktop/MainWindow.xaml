<Window x:Class="AttendanceAppProject.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AttendanceAppProject.Desktop"
        mc:Ignorable="d"
        Title="Student Attendance Desktop App" Height="550" Width="1000"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Label Content="Professor:" VerticalAlignment="Center"/>
            <TextBox x:Name="ProfessorNameTextBox" Width="150" Margin="5,0,0,0"/>
            <Label Content="Class:" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <ComboBox x:Name="ClassComboBox" Width="150" Margin="5,0,0,0">
                <ComboBoxItem Content="Class A"/>
                <ComboBoxItem Content="Class B"/>
                <ComboBoxItem Content="Class C"/>
            </ComboBox>
            <Button x:Name="CheckInButton" Content="Submit" Margin="10,0,0,0" Click="CheckInButton_Click"/>
            <Button x:Name="RefreshButton" Content="Refresh Data" Margin="10,0,0,0" Click="RefreshButton_Click"/>
        </StackPanel>

        <DataGrid x:Name="AttendanceDataGrid" Grid.Row="2" Margin="10" 
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="No." Binding="{Binding OrdinalNumber}" Width="60"/>
                <DataGridTextColumn Header="Student ID" Binding="{Binding StudentId}" Width="100"/>
                <DataGridTextColumn Header="Class" Binding="{Binding Class}" Width="100"/>
                <DataGridTextColumn Header="Check In Time" Binding="{Binding CheckInTime}" Width="150"/>
                <DataGridTextColumn Header="Quiz Question" Binding="{Binding QuizQuestion}" Width="*"/>
                <DataGridTextColumn Header="Quiz Answer" Binding="{Binding QuizAnswer}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StatusBar Grid.Row="3">
            <TextBlock x:Name="StatusTextBlock" Text="Ready"/>
        </StatusBar>
    </Grid>
</Window>