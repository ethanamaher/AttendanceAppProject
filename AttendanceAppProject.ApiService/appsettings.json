{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
      "DefaultConnection": "server=${ATTENDANCE_DB_SERVER};port=${ATTENDANCE_DB_PORT};database=${ATTENDANCE_DB_NAME};user=${ATTENDANCE_DB_USER};password=${ATTENDANCE_DB_PASSWORD};"
    }
}

